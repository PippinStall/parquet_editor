services:
  backend:
    build: ./backend
    ports:
      - "18010:8000"
    volumes:
      - ./backend/storage:/app/storage
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "18080:80"
    depends_on:
      - backend
    restart: unless-stopped
